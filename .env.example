# Server-side only — used by /api/edit-photo on Vercel. NOT exposed to the browser.
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: override the Gemini image model (defaults to gemini-2.5-flash-image)
# GEMINI_MODEL=gemini-2.5-flash-image
