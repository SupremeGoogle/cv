Place the hidden portrait reference at `public/me/me.png`.

The workplace AI demo reads this file internally from `/me/me.png`. It is not shown in the modal; the user only sees their workspace photo and the generated result.
